# COG 방향 예측 문제 분석

## 문제 현상
- 예측 시점에서 선박이 **북쪽(COG ~350도)**으로 이동 중
- 첫 예측: COG **359도** (북쪽) - 올바름
- 이후 예측: 점점 **동쪽(90-110도)**으로 변함 - 문제!

## 원인 분석

### 1. 훈련 데이터 편향
- 여수 항로 데이터: 주로 **동쪽(COG 평균 ~60-100도)** 방향
- 모델이 시간이 지나면서 훈련 데이터의 평균 방향으로 수렴

### 2. COG 미러링 로직
- `cog_mirror=True`로 훈련됨 (sin_cog 부호 반전)
- 미러링 로직 자체는 올바르게 작동
- 입력: 350도 → 미러링 → 10도 (모델 입력)
- 출력: 모델 예측 → 역미러링 → 359도 (첫 예측 올바름)

### 3. 테스트 결과
| 입력 COG | 예측 결과 |
|---------|----------|
| 60도 (동쪽) | 74→92→109→119도 (동쪽 유지) |
| 300도 (북서) | 309→284→251→...→90도 (동쪽으로 수렴) |
| 350도 (북쪽) | 359→4→24→57→97도 (동쪽으로 수렴) |

## 결론
- **미러링 버그가 아닌 모델의 일반화 한계**
- 훈련 데이터에 없는 방향은 평균 방향으로 회귀
- 선박의 실제 이동 방향이 다양하게 변하므로, 이를 반영할 수 있는 모델 개선 필요

## 개선 방안
1. 다양한 방향의 훈련 데이터 추가
2. 방향별 손실 가중치 조정
3. COG 변화량(각속도) 예측 방식 도입
4. 이전 COG를 더 강하게 유지하는 정규화 추가

---

## 구현된 해결책: Heading Inertia

### 1. 추론 시 적용 (trajectory_inference_v2.py)
`heading_inertia` 파라미터를 통해 이전 COG와 모델 예측 COG를 블렌딩:
```python
pred_cog = prev_cog + (model_cog - prev_cog) * (1 - heading_inertia)
```

### 2. 테스트 결과 (입력: 북쪽 350도)

| heading_inertia | 처음 5분 COG | 10-15분 COG | 25-30분 COG | 평균 편차 | 최대 편차 |
|-----------------|--------------|-------------|-------------|-----------|-----------|
| 0.0 (기본) | 350→3° | 85→138° | 130° | 100.7° | 148.4° |
| 0.3 | 348→0° | 52→109° | 129° | 90.4° | 142.1° |
| 0.5 | 347→358° | 33→67° | 127° | 76.5° | 139.1° |
| **0.7 (권장)** | 346→354° | 13→35° | 87→103° | **49.2°** | **112.7°** |
| 0.9 | 345→348° | 355→1° | 22→29° | 15.4° | 39.4° |

### 3. 권장 사용법
```python
inferencer.predict_multi_from_df(
    df,
    n_steps=30,
    heading_inertia=0.7  # 0.0~1.0, 높을수록 방향 유지력 강화
)
```

### 4. 주의사항
- `heading_inertia=0.9` 이상: 방향 변화에 너무 둔감해질 수 있음
- `heading_inertia=0.5~0.7`: 균형 잡힌 선택
- 실제 항로가 곡선인 경우에는 낮은 값 권장
