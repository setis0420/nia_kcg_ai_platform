# V13 선박 항적 예측 시스템 사용 가이드

## 개요

V13은 V12 모델을 기반으로 **수심 제약**과 **과거 항적 방향 반영** 기능을 추가한 후처리 보정 시스템입니다.

### 주요 기능
- **수심 제약**: depth >= 10m 영역만 항해 가능 (-99999 = 육지)
- **과거 항적 방향**: 학습 데이터의 실제 선박 이동 방향을 참조하여 예측 경로 보정
- **육지 회피**: 예측 경로가 육지로 향하면 자동으로 방향 전환

---

## 폴더 구조

```
배포용_V12/
├── models/
│   ├── 울산/
│   │   ├── model_best.pth
│   │   ├── config.pkl
│   │   └── track_grid.pkl    # V13 과거 항적 그리드
│   ├── 인천/
│   │   ├── model_best.pth
│   │   ├── config.pkl
│   │   └── track_grid.pkl
│   └── 목포/
│       ├── model_best.pth
│       ├── config.pkl
│       └── track_grid.pkl
├── trajectory_predictor_v12.py   # 기본 예측기
├── trajectory_predictor_v13.py   # 보정 기능 포함 예측기
└── generate_all_predictions_v13_mokpo.py  # 목포 예측 생성
```

---

## Track Grid 생성

### 방법 1: 전처리와 함께 자동 생성 (권장)

```bash
# 전처리 + track_grid 모두 생성
python preprocess_all_regions_v12.py

# 특정 지역만 처리
python preprocess_all_regions_v12.py --regions 울산 인천 목포
```

### 방법 2: Track Grid만 생성

이미 전처리가 완료된 경우:

```bash
python preprocess_all_regions_v12.py --track-grid-only

# 특정 지역만
python preprocess_all_regions_v12.py --track-grid-only --regions 목포
```

### 방법 3: 코드에서 직접 생성

```python
from trajectory_predictor_v13 import TrajectoryPredictorV13

predictor = TrajectoryPredictorV13(region='목포', enable_correction=True)
predictor.build_track_grid()  # models/목포/track_grid.pkl에 저장
```

---

## 사용법

### 기본 사용

```python
from trajectory_predictor_v13 import TrajectoryPredictorV13, predict_trajectory_v13

# 방법 1: 클래스 사용
predictor = TrajectoryPredictorV13(
    region='목포',
    device='cuda',  # 또는 'cpu'
    enable_correction=True  # V13 보정 활성화
)

result = predictor.predict(
    input_data=df,  # 30분 과거 데이터
    shiptype=70,    # 선종 코드
    length=150      # 선박 길이 (m)
)

# 방법 2: 함수 사용
result = predict_trajectory_v13(
    input_data=df,
    region='목포',
    shiptype=70,
    length=150,
    device='cuda',
    enable_correction=True
)
```

### 반환 결과

```python
result = {
    'predicted_coords': np.array,     # (60, 2) 보정된 예측 좌표 [lat, lon]
    'original_coords': np.array,      # (60, 2) 보정 전 원본 좌표
    'last_position': np.array,        # (2,) 마지막 입력 위치
    'shiptype_name': str,             # 선종명 (예: '화물선')
    'length_name': str,               # 길이 범위 (예: '120-160m')
    'correction_applied': bool,       # 보정 적용 여부
    'correction_distance_km': list,   # 각 점별 보정 거리 (km)
}
```

---

## 명령줄 옵션

### preprocess_all_regions_v12.py

| 옵션 | 설명 |
|------|------|
| `--regions 울산 인천 목포` | 처리할 지역 지정 |
| `--list` | 사용 가능한 지역 목록 출력 |
| `--track-grid-only` | 전처리 없이 track_grid만 생성 |
| `--skip-track-grid` | track_grid 생성 건너뛰기 |
| `--workers 4` | 병렬 처리 워커 수 |

---

## 수심 데이터

### 파일 위치
```
K:\coding_project\NIA_선박항적예측프로그램\depth_0.025deg_value.csv
```

### 데이터 형식
| 컬럼 | 설명 |
|------|------|
| cen_x | 경도 (lon) |
| cen_y | 위도 (lat) |
| depth | 수심 (m), -99999 = 육지 |

### 항해 가능 조건
- `depth >= 10m`: 항해 가능
- `depth < 10m`: 얕은 물 (항해 불가)
- `depth == -99999`: 육지 (항해 불가)

---

## Track Grid 상세

### 그리드 해상도
- 0.01도 (약 1km)

### 저장 데이터
```python
{
    'grid_resolution': 0.01,
    'mean_direction': {
        (lat, lon): {
            'cog': 평균_방향(도),
            'consistency': 방향_일관성(0~1),
            'count': 데이터_포인트_수
        },
        ...
    },
    'count_grid': {(lat, lon): count, ...}
}
```

### 방향 일관성 (consistency)
- 원형 분산 기반 (0~1)
- 1에 가까울수록 해당 그리드에서 선박들이 일관된 방향으로 이동
- 0.3 이상인 그리드만 유효로 판정

---

## 보정 파라미터

### PathCorrector 설정

```python
# 기본값
max_correction_per_step_km = 0.5  # 한 스텝당 최대 보정 거리 (km)
max_angle_change_deg = 20.0       # 한 스텝당 최대 방향 변화 (도)
smooth_window = 7                 # 스무딩 윈도우 크기
smooth_passes = 2                 # 스무딩 반복 횟수

# 파라미터 조정
predictor.path_corrector.set_params(
    max_correction_km=0.3,
    max_angle_deg=15,
    smooth_window=5,
    smooth_passes=1
)
```

---

## 지역별 좌표 범위

| 지역 | 위도 범위 | 경도 범위 |
|------|----------|----------|
| 울산 | 35.0 ~ 36.0 | 129.0 ~ 130.0 |
| 인천 | 36.5 ~ 37.8 | 125.5 ~ 127.0 |
| 목포 | 33.5 ~ 35.7 | 125.2 ~ 126.8 |
| 부산 | 34.8 ~ 35.5 | 128.5 ~ 129.5 |

---

## HTML 시각화

### 수심 격자 표시
- **갈색**: 육지 (depth = -99999)
- **노란색**: 얕은 물 (0 < depth < 10m)

### 항적 표시
- **초록색**: 과거 30분 실제 항적
- **회색 점선**: 보정 전 예측 경로
- **빨간색**: 보정 후 예측 경로

---

## 문제 해결

### Track Grid 로드 실패
```
[V13] 과거 항적 그리드 없음. 다음 경로에서 검색:
  - .../models/목포/track_grid.pkl
  - .../track_grid_목포.pkl
```

**해결**: track_grid 생성 실행
```bash
python preprocess_all_regions_v12.py --track-grid-only --regions 목포
```

### 좌표 폭발 (Coordinate Explosion)
예측 좌표가 비정상적으로 큰 값이 되는 경우, `_validate_coordinates()` 메서드가 자동으로 원본 좌표로 복원합니다.

### 선박이 육지로 예측되는 경우
- 수심 데이터 해상도(0.025도)가 예측 정밀도보다 낮아 발생
- `_avoid_land_trajectory()` 메서드가 궤적 전체를 회전하여 회피 시도
- 완벽한 해결이 어려우므로 시각화로 확인 권장

---

## 버전 히스토리

| 버전 | 주요 변경 |
|------|----------|
| V13 | 수심 제약 + 과거 항적 방향 반영 |
| V12 | 선종/길이 정보 포함 모델 |
| V11 | 30분→60분 예측 |
| V10 | Inertia-Aware Transformer |
